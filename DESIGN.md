# 视觉与行为基准

参考为目录原片的 5–40 秒，1920×1080、25fps。用户选择参考驱动的原生实现，不使用前端或动效 Skill。美术模型通过 Blender MCP 制作，界面使用 TypeScript、Three.js、Vite。

- 暖灰白底、黑色文字、暖杏金选中信号。界面以细线和紧凑排字组织，保留原片标题与右下角署名位置。
- 字体为用户指定的 MiSans，来自小米官方 https://hyperos.mi.com/font/download 的原版 WOFF2。使用 300、400、600、700 四个字重，font-display: swap；后备为 PingFang SC / Microsoft YaHei / system-ui。原版文件不转换、不子集化。版权与许可在 public/fonts，设置页含署名与许可入口。
- 5–27 秒为接入、身份验证、授权扫描、欢迎和白场。26.92–27.67 秒，阵列从左侧滑入，镜头从接近侧视快速抬高，随后进入斜俯视扫描。
- 27–29 秒为第一轮传播；29–30.5 秒反向回传。30.58 秒开始第一段小幅抽取，之后有停留；32.55 秒开始第二段抽取，镜头随后继续转向和推近至 39 秒。
- 选中卡片两侧在回传后继续起伏。主通道保留较高的连续肩部，其余通道幅度较低；按距选中位置的距离延迟并衰减。
- 相机校准必须使用未被遮挡的顶部边线、标签和重复卡片端点。不能把前排遮挡后的可见高度当作整张卡片高度，也不能通过扩大通道间距或设置通道高度阶梯补偿可见列数。
- 当前 31.48 秒镜头约为方位角 59°、仰角 19°、垂直取景范围 7.33 世界单位。远距镜头配合小视场角压缩透视。相邻通道同高、中心距 5.2；卡片宽 5、高 3.7，行距 0.62。后半段镜头转向与抽取升降分别控制。
- 39 秒特写由屏幕锚点控制构图，镜头逐渐转至约 18° 方位角、13.8° 仰角。保密等级扫描线投影在同一个实时模型上。
- 模型由磨砂盖板、基板、内部光学腔、环槽、边框、螺钉、标签与刻线组成；圆环位于盖板后方。重复卡片也启用物理透射，使用简化内部基板与象牙色端面。
- 交互保留检索、切换、详情、收藏、导出和设置。轨道与升降采用保留速度的临界阻尼；旧卡片独立归位，快速重新选中时接续运动。
- reference/review.html 仅在开发环境对照原片与实时场景；暂停使用原片逐帧图，播放使用视频。产品入口和生产构建不嵌入原片。

实时屏幕空间透射无法递归恢复原片全部透明层间折射。当前实现以关键帧投影和连续播放复核，尚未通过逐像素差分认证。

## 用户标注后的镜头与几何修订

- 三张标注经去除黄色笔迹区域后与原片帧匹配，对应 27.60、29.16、31.56 秒。
- 档案端面深度约 0.31、行距 0.62；原来的端面偏薄。由 Blender MCP 沿模型厚度方向调整并重新导出，标签与投影标记同时贴合新表面。
- 前半段采用较近的透视镜头：27.60 秒约 28 世界单位镜头距离、43° 仰角；29.16 秒约 35 世界单位距离、35° 仰角、67° 方位角。之后连续转至既有选中构图，特写镜头距离为 72。
- 回传波进入时机向后校正，29.16 秒尚未形成明显右侧回波，与标注帧对应。
- 删除所选模型朝 z=3.1 的位置插值，以及背景阵列随 detail 额外下沉的逻辑。选中槽位在抽取全过程固定横向与纵深位置，模型只有高度变化，镜头负责构图。

## 交互预览与进入详情

- 左右切换停在原片 31.56 秒对应的浅抬起状态（0.4 世界单位），邻排沿同一肩部轮廓排列；点击 ACCESS FILE 或按 Enter 后继续升至 3.35。返回恢复预览，详情中换选也先回到预览。
- 卡片超过浅抬起高度后，镜头才逐渐转向并把卡片放在画面左侧；详情文字随镜头进度在右侧渐显。抽取不再自动旋转模型，详情拖拽仍可检查角度。
- 阵列与抽出模型共用外壳几何，在同一个表面上随升降连续插值颜色、粗糙度与透光率；内部结构以连续覆盖率显现，文字标签渐显。每张归位卡片拥有独立材质状态，接近完全归位且外观一致后才恢复阵列实例。

## 最新交互修订（覆盖上文旧的按键和详情高度）

- 左右键切列，上下键切换列内前后档案。五列分别为工程研究、生命科学、机构档案、能量研究、特别项目，各 8 份，共 40 份；切回一列保留上次的档案选择。
- 760 帧（30.40 秒）各列波峰等高，随后摄像机右移，把同一物理列置于画面中央；785 帧附近非选中列仍保留 25% 以上的肩部波幅。列间突出程度采用连续插值。
- 交互详情升高 4.05 世界单位；预览仍为 0.4，原片时间轴的完整升高仍为 3.35。删除试调的 5.0 高度，保持邻片可见。
- 鼠标悬停时，对应物理位置的档案额外竖直上浮 0.18 世界单位，移开后回落；升起和回落使用 18 / 14 的临界阻尼。偏移在阵列、选中模型和归位副本之间连续复用，循环复用的同编号档案各自响应。保留悬停前的表面命中范围并逐帧更新目标，避免卡片升起后从静止指针下移开而闪动。进入详情时回落，原片时间轴不叠加；减少动态效果时立即切换悬停高度。
- 旋转限幅为 ±0.8 弧度。只有卡片完全避开邻片后才接受拖拽；返回先在原高度转正，再下降，镜头在转正期间维持构图。附近旧卡片转正时，新预览暂缓抬起，避免相互穿插。
- 标签和启动界面共用 src/brand.ts 的标志路径，标签不再手绘近似曲线。
- 档案正文为基于公开世界观资料的原创编目文章，不冒充游戏内原始档案。每份包含设定参考链接，详见 reference/LORE-SOURCES.md。
- 详情切换时，雾效距离跟随阻尼之后的实际镜头位置，避免进入时相机尚未到位、卡片提前被远雾覆盖。保留既有镜头轨迹与最终雾效。
- 交互预览停止操作 2.5 秒后渐入呼吸起伏：8 秒与 13 秒周期叠加，最大单向位移 0.102 世界单位（约卡片高度的 2.8%），相邻卡片略微错相。根据用户反馈，幅度由最初版本增至 3 倍，保留缓慢节奏。切换档案、进入详情或开启减少动态效果时渐退；原片时间轴不叠加此效果。
- 琥珀色侧条与透明盖板只有 0.002 世界单位的前后间距，斜视时会出现深度冲突条纹。仅为 Amber_Lightguide 材质增加 polygonOffset（factor=-1，units=-2），克隆归位卡片同时继承。保留原有相机裁剪范围、模型几何、材质色彩与全局 AO 参数。

## 界面显隐

- 首次进入交互界面后，SESSION AUTHORIZED 与右下 POWERED BY RHINE LAB 保持 5 秒，再用 1.2 秒淡出。切换预览和详情不重新计时；重播启动流程后重新计时。
- 上下箭头、档案刻度和快捷键提示共用感应区域，鼠标移开时用 300ms 隐藏，移回或键盘焦点进入时恢复。布局位置保持原样。
- 交互界面的左上品牌标识默认隐藏，鼠标移入或焦点进入时恢复完全显示。
- 主档案信息移除原 REFERENCE AREA 所在的范围标签，档案标题保持原字号。
- 中间档案信息在鼠标进入周围 100 个基准像素范围时逐渐淡化，内部最低保留 20% 不透明度；移远恢复，按钮继续可点击。距离随页面等比例缩放，键盘聚焦时完整显示。
- 无悬停能力的触控设备保持导航与中间信息完整显示；减少动态效果时立即切换透明度。启动参考时间轴沿用原来的品牌显示。
- 本轮构建通过；浏览器实测确认延时淡出、导航显隐、品牌透明度切换、信息靠近与移开、范围标签移除以及读取按钮可用。

## 独立模型查看器

- 详情页的「360° 查看文档模型」打开独立场景。沿用暖灰背景、既有字体、细线与克制的中英文字阶；不引入新的前端或动效设计方法。
- 鼠标左键拖动可完整环绕模型；上下左右键平移视角，滚轮或加减键缩放。Home 或「复位视角」恢复初始取景；ESC 或「返回档案」恢复原档案与键盘焦点。
- 「拆解档案」将紧固件、透明盖板、折射环组、光学核心、信息基板、背板与框架六组部件沿厚度轴分离。「一键重组」使各部件准确回到原位；途中反向操作保留当前运动位置与速度。减少动态效果设置下直接切换。
- 独立场景暂停主场景绘制，并复用校准后的材质、环境光配置与当前档案标签。原阵列模型与抽取镜头不受查看器变换影响。查看器的近裁剪面为 0.3，远裁剪面为 120，初始相机位置为 (7.2, 3.8, 12)，可缩放距离为 5–28。
- 部件资源由 Blender MCP 执行 art/build_assembly.py 生成：复用原建模脚本的几何，按物理部件和材质合并，导出带 assemblyPart 信息的 public/assets/archive-assembly.glb，并保留 art/archive-assembly.blend。
- 复现时通过 Blender MCP 在 Blender 内执行该脚本，并在执行作用域中传入脚本的绝对 __file__ 路径；脚本以自身位置定位项目。几何双向顶点距离检查允许 1e-5 的浮点误差，实测最大误差约 6.08e-7。
- 根据用户的内外光照对照，查看器改为在自己的 WebGL 上下文内生成相同的 RoomEnvironment/PMREM。禁止跨 renderer 直接复用 GPU 渲染目标纹理，否则环境光缺失会使内部基板和圆环发暗。背景统一为 #eae5e1，复用主界面的氛围渐变，雾效范围相对实际模型距离设为 -1 至 +12，使放大缩小时保留浅色磨砂质感。

## 循环档案阵列

- 主界面将内容编号与物理位置分开：五类档案和每列八份档案按周期映射到连续的行列坐标。上下、左右的方向操作每次改变一个物理位置；最后一项接续第一项时，轨道继续沿原方向运动。
- 切换列时保留该列的档案选择记忆，并选择附近的对应周期位置；点击场景卡片使用当前实例实际对应的档案与位置。
- 交互时维护 9 列 × 32 行、共 288 个位置，按镜头附近的可见窗口循环补位。主模型与归位副本按物理坐标独立管理，避免同一档案的不同周期副本互相覆盖。
- 横向采用整组阵列轨道平移，纵向保留原有前后轨道；两者都保留阻尼与速度。取景和灯光附近的实际坐标保持有限，选定后的抽取仍只改变高度。
- 当逻辑坐标超过 2048 时，按内容周期统一平移坐标与运动状态；保持世界位置、速度、波纹与呼吸相位连续，避免长期运行后浮点精度下降。
- 原片时间轴保留原始的前 160 个实例顺序与位置，额外列隐藏。循环后的重播会清理归位副本。

## 切换波浪微调（历史实验）

- 以循环阵列提交 aeddd8f 为基线。用户试用减弱波浪后，明确要求恢复原来的波浪幅度与节奏，仅去掉负向波谷。
- 交互选中波浪保留 0.8 幅度系数、0.2 秒起势、原衰减率和传播速度，仅把余弦的负值截为零。正向波峰仍会升起并回落，波浪本身不再将档案压低到基础肩部轮廓以下。
- 验证入口为 reference/rise-review.html；原片时间轴、浅抬起高度、详情抽取与归位规则沿用当前基线。
- 用户请求的可视对比页为 reference/wave-compare.html：两份实际 ArchiveScene 共用资源与时间步长。左侧可选择原始基线 aeddd8f 或仅去除负向波谷的 2048536，两者均为同时播放；右侧保留先抬起后波浪的实验方案。支持单步、连续、反向切换，0.25×/0.5×/1× 播放、逐帧定位、选中区域放大与高度曲线。每次回放检查实际位置与预先采样的轨迹一致。该页仅用于开发环境审阅。

## 先抬起，再波动（历史实验，当前不采用）

- 该实验的新选中档案先抬起，再发出选中波浪。预览目标仍为 0.4，向上抬起的临界阻尼速率从 4.2 调为 7.6；高度达到 0.35，且同列前后四格内的归位旧档案比它低至少 0.015 后，才启动波浪。单次正常切换约 0.48 秒后启动；旋转归位等特殊情况按实际状态等待。
- 每次选择替换尚未发出的波浪。进入详情、重播时取消待发波浪；减少动态效果时不发波浪。原片时间轴继续采用原值。
- 用户进一步要求消除选中档案再次跳起的效果：涟漪在源点的位移为零，并在周围 2.5 行的距离内平滑展开；正余弦包络改为平方，使边缘以零速度起落，保留远处波峰的幅度和传播速度。选中档案仅完成原定的 0.4 抬起，自己的涟漪不再给它叠加高度。
- reference/sequence-review.html 验证先后关系、归位高度、连续快速切换、反向、详情旋转归位、减少动态效果与重播取消。
- 档案序号与列编号采用 @kitlangton/rolling-number 0.4.1 的原生 DOM 接口，460ms 滚动，固定两位数，低强度运动模糊。方向键跨循环边界时保留滚动方向，快速输入由组件延续当前运动状态；系统减少动态效果与页面设置均可关闭滚动。MIT 许可随包保存到 public/licenses/rolling-number.txt。

## 当前采用：原始基线动效与光影微调

- 用户对比后明确回到原始基线 aeddd8f。生产场景默认使用完整的有正负波谷的选中波浪，选中与波浪同时开始，升降阻尼速率恢复 4.2。此前实验保留在参考页面；Rolling Number 沿用。
- 灯光统一到 src/archive-lighting.ts：曝光 1.00，环境强度 0.52，暖白主光 1.7，主光位置 (-8,14,4)，补光降至 0.3，半球光降至 0.5。主场景和独立查看器各自在自己的 WebGL 上下文生成环境贴图。
- 阵列盖板增加少量暖色吸收，斜面和背板偏向浅香槟色；内部圆环与基板保持可读的暖灰层次。吸收色和吸收距离与其他材质属性一起随升降过渡。阴影偏移小幅收紧，AO 半径从 0.38 调为 0.44。
- 模型、几何、布局与镜头沿用已确认版本。reference/light-review.html 可切换四个关键帧以及原视频/调整前参照；两侧实时场景使用相同模型位置与镜头，并自动检查一致性。原片关键帧需在本地 reference/motion 中准备，详见 verification/LIGHTING-POLISH.md。

## 基线配色与档案编号滚动

用户此前选择整体 baseline。生产灯光随后按满屏交互修订改为 warm，最新材质及阴影按下文透明亚克力与可选路径追踪修订；baseline 与 refined 继续作为参考对照。

X-001 形式的主档案编号和悬停编号复用现有 Rolling Number：固定 X- 前缀，三位数字保留前导零，460ms 滚动并支持快速输入衔接。方向键切换沿导航方向滚动；悬停首次出现直接显示，再切换目标时滚动；减少动态效果时立即更新。编号的字号和布局保持原样。

## 标题快切与 360° 查看器进出

主档案标题在连续变化间隔小于 240ms 时触发短促闪动，140ms 后变成稳定的黑色横条；横条锁定本轮开始时的宽度。停止 260ms 后以 180ms 短闪显示最终标题，单次切换直接显示。切入详情、启动或减少动态效果时清理计时和动画。

360° 查看器以 320ms 淡入覆盖原详情，页眉和页脚轻微上移就位；模型完成载入并渲染后以 380ms 从 0.97 倍展开并淡入。退出以 220ms 淡出和轻微收拢接回原档案。使用可取消的原生动画，进入途中按 Esc 从当前透明度退出；退出结束之前继续绘制、保持模态输入隔离，之后才释放模型、恢复详情和原按钮焦点。过期的异步载入结果按请求序号释放。减少动态效果时直接切换。

reference/micro-transition-review.html 验证真实应用中的快切、最终标题恢复、切入详情取消横条、进入打断、重复关闭、拆解中退出、焦点归还和重新载入。2026-09-08 实测 13 项检查全部通过。标题短时动画通过定位到动画末端检查，避免后台标签页对计时器的节流导致误判。

## 2026-09-08 · 启动前段逐帧修订

以原片 25 fps 连续帧复核 6.76–27.00 秒。用户排除黑底鹰角片头，启动和重播从画面变白后开始。应用的内部参考时间仍为原片时间减 5 秒；首次加载遮罩退出后才开始显示英文输入。

`boot-motion.ts` 将逐字输入、停顿、清空、闪切固定到原片帧点，空间位置使用连续插值。`boot.ts` 驱动 DOM/SVG，不依赖循环 CSS 动画的已播放时长，因此正常播放、慢放和前后跳帧使用同一画面状态。英文从固定左端输入，标志从右上弧线开始绘制、绕左环再补全右环，完成后向左移位；品牌三行错开出现。

身份段恢复原文 `ID CONFIRMED : JOYCE MOORE`、`REQUEST RECEIVED`、`START PROCESSING...`。权限段用从画面外收拢的黑白双环、反向内弧、橙色节点、四帧缩放跳变和中心图形切换。欢迎段保留黑白闪切、公司名称黑底横向扫过、第三行 `INTERNAL DATABASE` 闪现，最后缩小、失焦、带轻微青色过渡进入白场。进入阵列之后的 reveal/lift/zoom 时间函数未修改。

参考帧与检查结果见 `verification/BOOT-MOTION.md`。`reference/boot-review.html` 可正常播放、半速、四分之一速度和逐帧核对；原片及提取帧只用于本地验证，不作为产品背景。

## 2026-09-08 · 满屏交互与暖光

- 生产画布宽高随窗口变化，以统一缩放保留模型比例并铺满可用区域，移除固定 16:9 容器产生的留边。UI 缩放最低为 0.8，窄屏通过重新排布保持可读；`review=1` 继续使用 1920×1080 固定参考画布。
- 档案信息与切列导航共用 64% 的横向锚点，按最新反馈整体进一步右移。窄屏将信息放在中段，计数与切列置于下方，上下导航和快捷键另占一行。详情窄屏采用上方模型、下方滚动正文；调整取景由相机完成。
- 概览页 LOGO 之外的文字统一为 14 / 18 / 30 / 48 四档基准字号，所有档位按相同界面比例缩放。左上标识非焦点时完全隐藏。
- 点击不同物理位置的卡片先选择，再次点击当前选中的卡片直接打开。底部档案刻度同样支持再次点击读取；循环队列中相同编号的不同副本仍按各自位置选择。
- warm 灯光：曝光 0.99、环境 0.40、半球光 0.52，暖白主光 #ffe3b3 / 1.55 位于 (-7,13,2)，白色补光降至 0.36，增加 #f7cb7d / 0.32 的侧光。主界面与独立查看器分别生成相同环境贴图，沿用既有 Blender 资源和基线材质。
- 浏览器已检查 2048×1050、1280×720 与 390×844；主界面铺满，主要控件在视口内，概览字号为四档，模型再次点击进入正确详情。构建、档案运动约束与循环导航脚本通过。

## 2026-09-08 · 渲染稳定性

- 交互相机的近远裁切面随实际相机距离收紧至目标前后 60 个场景单位，避免原先 0.1–300 深度范围在远距离下无法分辨 0.002 的薄片间隔。参考时间轴保留原裁切范围。
- SSAO 在每帧绘制前同步相机投影、逆投影及近远裁切面，修正原先只在初始化或调整尺寸时更新的深度重建。AO 距离阈值换算后保持原有世界尺度。
- 后期处理在 OutputPass 前使用 SMAA 平滑边缘。它作用于实际输出画面，避免只设置 renderer.antialias、离屏后期画布仍无抗锯齿的情况；参考时间轴不启用该附加处理。
- 鼠标移动事件只记录最新坐标，渲染帧统一执行一次悬停拾取；点击仍立即确认当前目标。窄屏概览镜头固定对准档案位置，不再跟随悬停或待机波动上下移动；档案自身的抬升、待机和已确认波浪保持原样。
- `scripts/check-rendering.mjs` 使用交付模型验证循环阵列、入场和抽取状态的 165888 个边界点未被裁切，并检查薄片间隔在量化深度中可区分。浏览器复核了悬停、连续切档/切列、详情净空与返回；运行时 `aoProjectionSynced` 为 true，浏览器无渲染错误。

## 2026-09-08 · 档案 UI 过渡重做

- 交互 UI 由 `archive-ui-motion.ts` 的同一状态驱动，沿用主渲染帧，不另启计时循环。概览先退场，详情根据实际镜头进度依次显示标题、分隔线、编目信息、正文与操作；位移控制在 4–10 个基准像素，不使用大幅缩放或模糊。
- 概览在详情退场完成前保留相同的响应式位置与字号。修正原先切换 `data-mode` 后回退到旧布局、切列导航立即消失，以及详情立刻设置 hidden 导致的闪现。详情完成淡出后再隐藏，返回时等镜头退回再恢复概览。
- 阅读底色使用独立透明层与概览氛围交叉淡入，宽屏横向、窄屏纵向过渡。进入、返回或重新选取只修改动效目标，保留当前可见状态，支持连续打断。
- 单次档案标题变化以 260ms、4px 轻淡入承接数字滚动；快速输入仍保留已确认的黑条反馈。详情标签页以 220ms 淡入更新。模型、阵列波浪及 360° 查看器原有进出节奏不变。
- 未显现的内容保持 inert。详情正文可读后再接收焦点，退出后恢复原档案入口；打开独立查看器时暂停底层 UI 更新，保留模态焦点隔离。减少动态效果立即应用目标状态，启动重播清除交互样式。
- `scripts/check-ui-motion.mjs` 覆盖分层顺序、24 次过渡打断、30/120Hz 一致性、减少动态效果与重播复位。浏览器验证 Enter → Esc → 重选 → Enter 可到达正确档案；进入与退出时概览横向位置和 30px 标题不跳变，淡出期间详情保留，结束后隐藏并恢复焦点。用户确认闪现已消失。

## 2026-09-08 · 白色磨砂与实时柔影（历史实验，已退出生产）

用户试用后要求全部外壳恢复透明亚克力。以下保留实验的复现记录，生产入口不再加载磨砂模型或 `RayShadowPass`。工作目录清理后，实验源文件、资产与专用检查已移入 `archive/frosted-shadow-experiment-2026-09-08.zip`；以下路径为恢复到项目根目录后的路径，见 `archive/README.md`。

- 按用户的新要求，通过运行中的 Blender MCP 烘焙可重复的细磨砂法线与粗糙度贴图，导出 `public/assets/archive-cassette-frosted.glb`。`art/rhine-frosted.blend` 保留材质节点、程序化烘焙场景和打包贴图；`art/make_frosted.py` 通过 `python scripts/blender-mcp.py art/make_frosted.py` 重现。原始模型与 11 组网格几何不变。
- 外框使用白色磨砂表面，盖板保留适度透光。粗糙度因子分别为盖板 0.48、外框 0.62，乘以烘焙的 0.84–1.0 微表面贴图；法线强度 0.22、UV 重复 7 倍、IOR 1.46、薄涂层 0.035。移除生产磨砂材质原来按高度染棕与强制镜面粗糙度的着色器覆盖。阵列与选中模型共享纹理，透光参数沿现有抽取进度连续过渡；独立查看器复用同一套材质。
- 主界面高质量模式增加 `RayShadowPass`：GPU 在世界空间追踪每像素 8 条固定面积光阴影射线，对随模型运动更新的有向遮挡盒 BVH 求交。遮挡体来自不透明内衬，包含屏幕外的可见窗口实例和归位副本。半分辨率计算，上限 960×640，再按深度引导合成。没有逐帧随机噪声或时间累积，不因切档留下拖影。
- 这是阴影体近似的混合渲染，不是三角形级完整路径追踪，也不调用 RTX 硬件光追或计算多次反弹全局光照。面积光可见度按法线与雾衰减合入现有照明，SSAO 保留近距离接触层次；光追启用时关闭主光阴影贴图以避免双重变暗。关闭高质量模式使用 PCF 实时阴影；原片时间轴和参考材质沿用旧方案。
- 独立查看器的拆解部件有圆环与开孔，使用实际网格的 2048² PCF 阴影，以保留孔洞形状及部件自阴影。没有将这些部件当作实心盒处理。查看器旋转、拆解和原有进出动画保持不变。
- 验证方法与运行结果见 `verification/FROSTED-SHADOWS.md`。设置中高质量渲染的说明同步注明实时光追柔影。

## 2026-09-08 · 透明亚克力、清晰度与可选 GPU 路径追踪

- 全部外壳恢复原始 `archive-cassette.glb` 与 `archive-assembly.glb`，继续使用已有 Blender MCP 源模型。生产盖板粗糙度 0.065、透射 0.9，外框粗糙度 0.12、透射 0.88；阵列与抽取之间仍在同一网格连续插值。生产透明材质不再按高度强制改为磨砂；磨砂烘焙资源仅作为历史实验保留。
- 主阵列移除盒状阴影体后期叠黑，恢复真实不透明部件的 4096² PCF 阴影与原有 SSAO。warm 灯光的方向、颜色与曝光沿用满屏修订；阵列内衬恢复暖棕层次。模型附近雾效后移，避免把透明层次冲白。
- 交互页关闭 Bokeh 景深，原片时间轴保留景深。渲染目标按窗口实际显示面积与设备像素比设置，支持原生 4K，小型低 DPI 视口适度超采样，上限 1200 万像素。高质量后期目标使用最多 4× MSAA，并保留 SMAA 与此前裁切面精度修复。
- 独立查看器新增可选「GPU 光追」，采用 `three-gpu-pathtracer@0.0.24` 与 `three-mesh-bvh@0.9.14`。使用真实部件三角形、6 次常规反弹、12 次透射反弹、3 盏暖色面积光和同源环境光，保持全输出分辨率；通过 2×2 分块逐步采样，最多 512 samples，并使用库内 glslSmartDeNoise 保边降噪。
- 相机运动和拆解动画期间显示实时画面；停止后更新相机或部件 BVH，累积足够样本后用 500ms 淡入光追图像。进入、关闭、重新载入保留原有过渡与焦点规则。查看器关闭后释放路径追踪资源。
- 这是运行在实际 GPU 上的 WebGL2 路径追踪，不直接调用 RTX 专用核心。此轮先交付独立查看器；随后按用户要求增加首页支持，见下文。验证与已知限制见 `verification/ACRYLIC-PATHTRACING.md`。

## 2026-09-08 · 首页 GPU 光追

- 用户要求首页也支持光追。顶部新增「GPU 光追」开关，默认开启并记忆选择；状态只显示预览、准备、采样、细化和就绪。原片参考入口不启用首页光追。
- 使用同一 `three-gpu-pathtracer`。静止后，将 288 个循环位置中的实际非零实例展开成共享原始 Blender 几何的 Mesh 快照；保留世界变换、隐藏实例的归属和抽取高度，包含选中的完整模型与地面。没有用遮挡盒代替几何。当前首页为 1435 个非零材质实例、约 262 万个三角形。
- BVH 首次构建在 `GenerateMeshBVHWorker` 中执行，几何移动后重新生成或 refit。异步任务带修订号：任务期间继续操作时旧结果不进入显示，下次停稳后使用最终档案。Vite 排除 worker 入口预打包，开发与生产均输出有效工作线程资源。
- 启用后保留切档波浪、悬停 0.18、预览 0.4、详情 4.05 和相机动画。持续待机波浪平缓归零，以允许采样积累。运动时显示实时渲染，弹簧和波浪停稳后等待 400ms，再更新快照。达到 12 samples 后用 650ms 淡入；8 次常规反弹、20 次透射反弹，全分辨率 3×2 分块采样，上限 512，完成后不重复绘制同一光追结果。
- 使用三盏远置暖色面积光与同源环境，计算真实遮挡、反射、透射与间接光。外壳采用透明亚克力；移除实时渲染所需的过强深色补偿，避免在真实遮挡下重复变暗。保留原始几何，光追用的 RGBA 顶点色只写入运行时副本。
- glslSmartDeNoise 降噪后，根据当前深度恢复首页原有雾效，再进行色调映射；UI 继续由 DOM 绘制。开发服务器已更新，验证见 `verification/HOME-PATHTRACING.md`。

## 2026-09-08 · 暖光层次与信息区定位

- 按用户的新要求，生产 warm 曝光从 0.99 降至 0.88；环境补光 0.27、半球光 0.34，主光 #ffdead / 1.6，补光 0.22，边缘光 #f5c177 / 0.3。主光方向继续为 (-7,13,2)。两套光追环境强度统一为 0.34；实时预览和光追均从 `WARM_STUDIO` 读取配置。弱化环境填充以保留阴影，同时通过暖色主光和亚克力边缘反光表现层次。生产概览边缘雾层改为更淡的暖灰，避免大面积冲白；原片参考的 baseline 参数不变。
- 首页和独立查看器自动开启 GPU 光追，移除开关与采样状态文字。旧设置中的关闭值不覆盖此次要求。内部 DOM 状态继续用于验证，动画时实时预览、停稳后累积与淡入的节奏保持不变。
- `archive-layout.ts` 将左侧 56% 作为模型区域边界，右侧区域止于安全边距，以该区域中点作为信息区中轴。使用 MiSans 30px 标题的实际测量宽度加操作留白，限定块宽 440–520 基准像素；再约束整块不能超过屏幕安全边距。分隔线不再一直延伸到屏幕边缘，切列导航按同一中轴定位。这是结合当前构图的空间分配，不宣称存在通用的人类视觉最优坐标。
- 可用宽度小于 960 时使用居中可读区，小于 720 时切列导航为左侧计数保留空间。定位只在载入字体和窗口尺寸变化时重算；概览与详情退场共用相同位置，不改变已确认的过渡状态机。
- 详情页左上 LOGO 使用 `display: none`，不能再由悬停显示。ARCHIVE OVERVIEW 和系统导航使用相同 top 与 44px 高度；窄屏统一为 32px 顶边，搜索缩为图标并保留可访问名称。
- 构建与原有 UI 过渡检查通过，另检查 39 组宽度和标题测量组合的边界与中轴约束。浏览器实测 1176×1272 概览：信息区横向 723.05–1075.05，切列导航同中心 899.05；390×844 详情的返回与 SAVED 均为 y=25.6、h=35.2 显示像素，LOGO 的布局尺寸为 0。无光追控件时，实际三角形场景仍在 Worker 构建后进入 refining，实测 72.17 samples。可用 `verification/layout-review.html` 复核其他尺寸。
- 本轮手机检查后预览工具连接中断，未完成新增 1920 / 2560 宽屏的浏览器复核；上述宽度计算覆盖这些尺寸，不将计算检查记为已完成的实机检查。

## 2026-09-08 · 详情字号与模型旁说明

- 详情页将零散字号统一为 14 / 18 / 30 三档：编目标签、序号、辅助说明与操作文字为 14，正文、中文标题、编目内容与标签页名称为 18，英文标题和 `NO.XXX` 为 30。调整长标题换行、编目间距及正文 1.8 倍行距；操作栏在窄屏换行，避免字号增大后拥挤。
- 按用户截图移除 `DRAG TO INSPECT` 与「360° 查看文档模型」两项 UI。模型旁只保留 `NO.XXX` 和下方的 `INTERNAL DATABASE`，宽屏距底部 96 基准像素，紧凑布局距底部 52 基准像素，保持竖排。原有三维拖拽与独立查看器源码、模型资源未删除。
- 这些样式与当前 mode 解耦，退出详情时不回退到旧字号或旧位置。保留 `.object-caption` 作为原有动效目标，不改动已确认的过渡状态机。
- TypeScript 与 Vite 生产构建通过；本轮预览工具仍断连，尚未完成新字号的截图复核。

## 2026-09-08 · 按原片截图校准光照

- 用户提供新的原片截图作为光影目标。本地 checkout 不含原视频与原先提取的连续帧，本轮依据该附件校准：其背景采样为 RGB (235,231,227)，暖色集中在衬层透光与少量侧边高光，而非整幅黄色滤色。
- 生产 `WARM_STUDIO` 曝光 0.96，环境强度 0.42，光追环境 0.58，半球光 0.5。主光 #fff5e8 / 1.65 位于 (-8,14,3)，近中性补光 0.36；暖色侧光 #ffd593 / 0.55 位于 (-8,5,8)。共享 RoomEnvironment 增加 2.0 的中性环境补光，减轻暗墙在亚克力上的黑色反射；原片 baseline / refined 不应用该补光。
- 修正光追快照未传递 `Mesh.castShadow` 的差异：该库实际读取材质上的同名标志。现在显式传入源网格的设置，透明表皮不额外遮挡阴影射线，内部不透明件继续以真实三角形投影，镜面反射与透射仍保留外壳。独立查看器沿用相同规则。
- 光追阵列衬层使用 #cbb497，将暖色放在外壳内部；边框透射 0.76、粗糙度 0.16、非金属，保留透光并减轻掠射角下的过硬黑边。盖板仍使用原始透明材质与几何。未修改 Blender 资源、相机、抽取运动或 UI。
- 检查与实际 GPU 采样记录见 `verification/REFERENCE-LIGHTING.md`。

## 2026-09-09 · 同步上游开场、外壳与解密

- 接入上游 `89c7742`、`61e9fc0`、`f8e0f6f`、`56ca970`、`dde61fd` 的实际实现。此前两项提交只有祖先关系而没有代码；本次保留本站模板引擎，逐项接入其功能，使用正常合并记录上游关系。
- 开场使用实测非线性轨迹：连续轮廓同时移动绘制起点和终点，扫描环分层转动，六颗卫星点依次出现，加减符号展开；欢迎文字在灰闪帧保留遮罩与反相内容。自定义品牌、标志、公司名称及身份文字继续通过站点配置注入。
- 保留上游 Blender 源文件、生成脚本及两份 GLB。内构采用锐利折边、封闭光学薄层和连续贝塞尔连接带；外壳采用齐平黄色嵌片、右上／左下螺丝与盖板后的刻线。新增细节只放在抽取模型，循环阵列仍采用固定 288 实例位置，原片参考仍为 160 个位置。
- 抽取接近就位后，解密线在同一模型空间对角线上伸展、停留、收束；盖板在原片 38.84–39.56 秒由顶部向下变清晰，羽化带为高度的 24%。交互压缩为约 3.63 秒，减少动态效果直接清晰。归位副本保留独立清晰度，重新选择沿用当前高度；旋转、转正与竖直升降约束不变。
- 盖板每帧更新物理粗糙度、透射与厚度，光追快照在复用网格时刷新这些参数。实时渲染用不透明捕获中的透明混合近似展示内构，光追则使用独立的物理透射材质。动画期间显示实时前沿，结束后再累积，避免旧光追帧覆盖解密动画。
- 新增封闭光学层需要更多路径穿越，两处光追预算统一为 16 次常规反弹、32 次透射穿越，避免路径终止在环内形成黑块。修正降噪合成的预乘透明度顺序，淡入时对颜色与透明度同时加权，避免过曝；保留既有暖白灯光参数。
- 主相机的动态深度范围保留，并将近裁剪面下限设为 5；原片参考使用 near=5 / far=300。上游 11 个镜头包围盒检查重新运行，确保模型不被新增近裁剪面截断。
- 独立查看器内部增加清晰／磨砂连续切换，支持拆解中切换与快速反向。产品详情页不恢复已移除的查看器入口；两个站点的 Markdown、专题数据、阅读布局和独立存储保持原样。
- 本分支实测与复现命令见 `verification/UPSTREAM-SYNC.md`；上游测量文档单独标注出处，不混作本分支的新验收结果。
